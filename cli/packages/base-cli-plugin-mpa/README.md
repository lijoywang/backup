## base-cli-plugin-mpa

> MPA(multiple-page application) plugin for mfw-cli

mfw-cli 多入口项目插件

#### Options

```js
{
  page: {
    root: [string],
    pages: [string],
    file: [string],
    exclude:[string]
  },
  selectEntry: [boolean]
}
```
